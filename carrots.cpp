#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    scanf("%d %d", &a, &b);

    string c;
    getline(cin, c);
    
    while (a-- && getline(cin, c)) {};

    printf("%d\n", b);

    return 0;
}